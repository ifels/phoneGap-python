var getNetStateInfo = function() {
    UVPlatform.exec(null, null, "NetState", "getStateInfo", ['paremet1', 'paremet2','测试3']);
};

function init() {
    //document.addEventListener("deviceready", deviceInfo, true);
}
